# 目录治理规范

> 目标：把本项目整理到“结构纪律严明、整体中文、职责单一、长期可维护”的状态。  
> 适用范围：主工程目录。`eyes` 作为外部仓库区，不纳入本规范的强制中文改造范围。

---

## 一、治理目标

本规范解决四个问题：

1. 什么应该放根目录
2. 什么应该进入输入、输出、缓存、日志、临时、记忆等专属目录
3. 什么是主工程，什么是外部仓库
4. 中文目录如何在保持清晰的同时不破坏现有运行逻辑

---

## 二、目录治理结论

### 结论 1：主工程应以中文目录为治理目标

主工程未来应逐步收敛到中文治理目录。  
中文目录的好处是统一认知、降低维护理解成本、让目录本身承担“说明书”的作用。

### 结论 2：第三方项目不强制中文化

以下目录短期不建议直接改名：

1. `hermes-agent`
2. `openclaw`
3. `grafana`
4. `staroffice-ui`
5. `letta`
6. `eyes` 下所有外部仓库

理由很简单：这些目录要么是第三方源码，要么可能被脚本、配置、依赖路径直接引用。  
因此，治理方式应是“上层中文归类”，不是“一刀切重命名第三方仓库”。

### 结论 3：先建立中文治理层，再逐步迁移

推荐治理顺序：

1. 先写清规范
2. 先统一新文件流向
3. 再收口根目录散落文件
4. 最后再处理可安全迁移的目录

---

## 三、推荐顶层结构

下面是一份适合当前项目的推荐结构。  
不是要求一次改完，而是作为最终收敛方向。

```text
项目根目录/
├── README.md
├── DESIGN.md
├── DEPLOYMENT_REPORT.md
├── INTEGRATION.md
├── USER_MANUAL.md
├── 项目中心记忆.md
├── 目录治理规范.md
├── 启动/
│   ├── start_all.bat
│   ├── start_all.ps1
│   └── setup.bat
├── 输入/
│   ├── README.md
│   ├── 原始配置/
│   ├── 待处理数据/
│   └── 外部输入/
├── 输出/
│   ├── README.md
│   ├── 正式交付/
│   ├── 阶段成果/
│   └── 导出结果/
├── 缓存/
│   ├── 下载缓存/
│   ├── 渲染缓存/
│   └── 索引缓存/
├── 日志/
│   ├── 服务日志/
│   ├── 巡检日志/
│   └── 任务日志/
├── 临时/
│   ├── 调试中间件/
│   ├── 临时导出/
│   └── 待清理文件/
├── 记忆/
│   ├── 每日/
│   ├── 每周/
│   ├── 每月/
│   ├── 长期/
│   └── 决策记录/
├── 文档/
│   ├── 架构/
│   ├── 部署/
│   ├── 运维/
│   └── 扩展/
├── 脚本/
│   ├── 自研工具/
│   ├── 运维脚本/
│   └── 检查脚本/
├── 监控/
│   ├── grafana/
│   └── staroffice-ui/
├── 核心引擎/
│   ├── hermes-agent/
│   ├── openclaw/
│   └── letta/
├── 工具/
│   └── tools/
└── 外部仓库/
    └── eyes/
```

---

## 四、与当前项目的映射建议

当前项目已经存在若干目录，因此更实际的做法是分为“立即生效”和“后续迁移”两类。

### 立即生效

这些规则从现在开始就可以执行：

1. `input` 视为 `输入`
2. `output` 视为 `输出`
3. 新增缓存统一进入未来的 `缓存`
4. 新增日志统一进入未来的 `日志`
5. 新增临时文件统一进入未来的 `临时`
6. 新增长期原则、决策、沉淀统一进入 `项目中心记忆.md` 或未来 `记忆`
7. `eyes` 明确视为外部仓库区

### 后续迁移

这些内容适合在不影响运行的前提下分步整理：

1. 把散落根目录的说明文档逐步归类进 `文档`
2. 把启动脚本收口到 `启动`
3. 把 `tools` 的运行日志、中间产物剥离到 `日志`、`临时`
4. 把 `grafana` 与 `staroffice-ui` 在认知上收口到 `监控`
5. 把 `hermes-agent`、`openclaw`、`letta` 在认知上收口到 `核心引擎`

注意：这里强调“认知收口”优先，不要求一开始就真实改目录名。

---

## 五、每类目录的硬规则

### `输入`

只允许放未处理输入，不允许放最终输出。  
典型内容：

1. 原始配置
2. 原始数据
3. 外部导入资料
4. 模板

### `输出`

只允许放正式结果或对人可读的阶段成果。  
典型内容：

1. 报告
2. 导出页面
3. 可交付文档
4. 最终图表

### `缓存`

缓存必须满足“删了还能重建”。  
缓存不得作为唯一数据来源。

### `日志`

日志是运行记录，不是设计文档，不是业务输入，也不是长期记忆。

### `临时`

临时目录内容默认可删除。  
任何文件在 `临时` 存放超过一个阶段，就要重新判断是否归档到别处。

### `记忆`

记忆目录用于长期保留思想、策略、蒸馏结果、决策演进。  
不允许把一次性调试结果伪装成“记忆”。

---

## 六、根目录收口规则

根目录只允许保留四类内容：

1. 总览级文档
2. 全局启动入口
3. 一级功能目录
4. 少量确有必要的全局配置

建议逐步减少根目录中的零散文件。  
尤其禁止未来继续把无归属的：

1. JSON
2. HTML
3. SVG
4. 调试脚本
5. 测试产物

直接放在根目录。

---

## 七、中文目录策略

### 可以坚持中文的部分

以下目录最适合中文化：

1. 输入
2. 输出
3. 缓存
4. 日志
5. 临时
6. 记忆
7. 文档
8. 启动
9. 监控
10. 核心引擎
11. 外部仓库

### 不建议强行中文化的部分

以下目录不要贸然改名：

1. 第三方仓库根目录
2. 依赖明确引用的源码目录
3. 包管理器识别的项目目录
4. 被脚本硬编码路径引用的目录

因此，正确策略不是“全面改中文”，而是：

1. 治理层中文
2. 第三方层保留原名
3. 通过文档统一认知
4. 对自研新增目录严格中文化

---

## 八、eyes 的独立处理

`eyes` 目录按以下规则单独处理：

1. 它属于外部仓库区
2. 不要求中文目录
3. 不要求与主工程目录风格统一
4. 可按需清理子项目内 `.git`
5. 可清理缓存、构建产物、无用测试垃圾
6. 正式吸收某个项目时，再做单独归档和治理

这意味着主工程追求“结构纪律严明且中文”，并不等于必须去改造 `eyes` 里的外部项目。

---

## 九、执行顺序建议

如果后面开始实操整理，建议按这个顺序：

1. 先把“新增文件去哪”定死
2. 建立 `缓存`、`日志`、`临时`、`记忆`
3. 让新产物停止继续散落
4. 再搬运旧文件
5. 最后处理脚本路径与文档引用

这样风险最小，也最容易保持项目一直可运行。

---

## 十、后续整理时的判断标准

任何目录调整都应满足以下四条：

1. 看名字能猜到用途
2. 与相邻目录没有职责重叠
3. 不因单次任务临时需要破坏整体结构
4. 新人第一次看项目也能理解

如果一个目录需要靠口头解释才能明白，就说明它还不够好。

---

## 十一、当前最重要的落地结论

结合你的要求，当前最重要的结论就是：

1. 主工程追求中文、严格、单义目录体系
2. `eyes` 不纳入这个强制范围
3. 先建立“项目中心记忆”作为统一思想中心
4. 以后所有目录演进都以本规范和中心记忆为依据

---

## 十二、变更记录

### 2026-06-22

1. 新建目录治理规范
2. 明确 `eyes` 为外部仓库区
3. 确立“主工程中文治理、第三方仓库保留原名”的处理策略
4. 给出适配当前项目的长期目标目录结构
