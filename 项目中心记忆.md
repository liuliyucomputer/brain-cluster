# 项目中心记忆

> 用途：作为本项目长期统一思想、目录纪律、决策边界与演进原则的唯一中心记忆文件。  
> 维护原则：凡涉及目录调整、核心定位变化、模块职责变化、长期规范新增，优先更新本文件，再改其他文档。  
> 地位：高于零散说明，低于实际可运行代码。

---

## 一、项目定位

本项目是一个持续运行、可观测、可进化的多 Agent 系统工程，而不是单次执行的脚本集合。

项目的核心目标不是“能跑一次”，而是：

1. 长期稳定运行
2. 结构清楚，便于维护
3. 模块职责明确，边界稳定
4. 记忆、执行、观测三条主线长期统一
5. 新增内容不破坏整体秩序

---

## 二、最高原则

### 1. 目录必须服务认知

目录不是给机器看的备注，而是给人快速理解项目用的。  
一个目录放什么，必须从名字就能大致判断。

### 2. 中文优先，术语例外

本项目要求总体目录体系以中文为主，便于长期维护与统一认知。  
但以下情况允许保留英文：

1. 第三方外部仓库
2. 上游开源项目原始目录
3. 编程语言、框架、协议、工具链的标准术语
4. 已被大量脚本硬编码依赖、暂不适合立刻改名的路径

结论不是“英文绝对禁止”，而是“主工程治理层必须中文，外部依赖层可保留原貌”。

### 3. 主工程与外部仓库分治

`eyes` 目录视为外部仓库观察区、候选能力区、参考工具区。  
它不是主工程结构的一部分，不参与主目录纪律评价。  
因此：

1. `eyes` 下目录命名不要求中文统一
2. `eyes` 下可保留上游仓库原始结构
3. `eyes` 下各项目的 `.git`、缓存、示例产物可以按需要清理
4. 主工程文档不应把 `eyes` 的松散结构误写成主项目标准

### 4. 运行产物必须归位

任何新增文件都必须先回答一个问题：它是源码、配置、输入、输出、缓存、日志、临时文件，还是记忆沉淀？  
如果回答不出来，就说明该文件不该直接落到根目录。

### 5. 根目录必须克制

根目录只允许放：

1. 项目总览型文档
2. 全局启动入口
3. 一级模块目录
4. 少量确有必要的全局配置

禁止把临时脚本、阶段产物、实验结果直接堆在根目录。

---

## 三、主工程边界

当前主工程应以以下部分为核心：

1. `hermes-agent`：执行底盘
2. `openclaw`：能力/记忆相关引擎
3. `tools`：自研工具与编排
4. `grafana`：观测基础设施
5. `staroffice-ui`：监控界面
6. `input`：输入区
7. `output`：输出区
8. `letta`：记忆分页相关数据与接入
9. 根目录关键文档：总览、设计、部署、手册

`eyes` 为外部能力区，不纳入上述主工程纪律评分。

---

## 四、长期目录思想

本项目未来应逐步形成“中文治理层 + 原名保留的第三方层”的双层结构：

### 中文治理层

用于表达本项目自己的组织方式、数据流向、维护纪律。

推荐长期存在的中文目录概念：

1. `输入`
2. `输出`
3. `缓存`
4. `日志`
5. `临时`
6. `文档`
7. `脚本`
8. `配置`
9. `记忆`
10. `监控`
11. `核心引擎`
12. `外部仓库`

### 原名保留层

用于保留第三方仓库原貌，减少改名带来的兼容成本。  
比如 `hermes-agent`、`openclaw`、`grafana` 这类上游项目，短期内不强制中文化目录名，但在中文治理文档中必须明确其职责。

---

## 五、目录纪律

### 根目录允许项

根目录只应保留：

1. `README.md`
2. `DESIGN.md`
3. `DEPLOYMENT_REPORT.md`
4. `INTEGRATION.md`
5. `USER_MANUAL.md`
6. `项目中心记忆.md`
7. `目录治理规范.md`
8. 一级功能目录
9. 全局启动脚本

### 根目录禁止项

以下内容不应长期留在根目录：

1. 临时测试脚本
2. 中间分析结果
3. 版本试验输出
4. 临时截图
5. 单次任务日志
6. 无归属的 JSON、HTML、SVG、CSV

---

## 六、文件分类原则

### 输入

外部喂给系统但尚未加工的内容。  
例如：

1. 原始配置模板
2. 用户输入材料
3. 待处理数据
4. 外部导入样本

### 输出

最终交付物、可查看结果、阶段性正式产物。  
例如：

1. 正式报告
2. 导出页面
3. 对外结果文件
4. 已确认的架构升级文档

### 缓存

可删除、可重建、用于提速的文件。  
例如：

1. 模型缓存
2. 临时索引
3. 渲染缓存
4. 下载缓存

### 日志

运行过程记录，不属于业务源码。  
例如：

1. 服务运行日志
2. 巡检日志
3. 任务流水
4. 错误追踪

### 临时

处理中间态，任务结束后可清理。  
例如：

1. 调试文件
2. 过渡脚本
3. 临时导出
4. 待合并中间件

### 记忆

面向长期积累的策略、原则、决策、沉淀信息。  
例如：

1. 每日记忆
2. 每周蒸馏
3. 每月策略
4. 长期经验库
5. 本文件

---

## 七、命名原则

### 目录命名

主工程治理目录统一使用中文，要求：

1. 简短
2. 单义
3. 可长期沿用
4. 不混用中英表达同一层级含义

建议：

1. 用 `输入`，不要同时再有 `input`
2. 用 `输出`，不要同时再有 `output`
3. 用 `缓存`，不要同时再有 `cache`
4. 用 `日志`，不要同时再有 `logs`

### 文件命名

文档文件建议：

1. 用中文名表达用途
2. 保持稳定，不频繁改名
3. 同类文档使用统一前缀

例如：

1. `项目中心记忆.md`
2. `目录治理规范.md`
3. `部署报告.md`
4. `集成手册.md`

---

## 八、对 eyes 的明确约定

`eyes` 为外部仓库区，规则如下：

1. 不作为主项目目录整洁度评估对象
2. 可保留英文目录
3. 可保留上游仓库结构
4. 允许删除各子项目内无用 `.git` 目录与冗余缓存
5. 若某个 `eyes` 项目正式并入主工程，必须先经过“归档、改名、重定位、职责声明”四步

正式并入前，不直接把 `eyes` 某个仓库视作主工程规范样板。

---

## 九、文档关系

长期建议建立如下关系：

1. `README.md`：给首次进入项目的人看
2. `DESIGN.md`：讲系统设计与演进
3. `目录治理规范.md`：讲目录规则与落地结构
4. `项目中心记忆.md`：讲长期原则、边界、统一思想
5. 其他手册：讲具体模块、部署、使用

其中：

- `README.md` 负责入口
- `DESIGN.md` 负责设计
- `目录治理规范.md` 负责结构
- `项目中心记忆.md` 负责长期一致性

---

## 十、每次改动前的自检问题

任何新增目录或文件之前，先问：

1. 它属于哪一类：源码、配置、输入、输出、缓存、日志、临时、记忆？
2. 为什么必须放在这里？
3. 三个月后别人还能一眼看懂吗？
4. 这个名字是否能长期稳定？
5. 是否会和现有目录职责冲突？
6. 是否应该先更新本中心记忆或目录治理规范？

只要其中两项答不上来，就先不要落盘。

---

## 十一、禁止事项

长期禁止以下行为：

1. 为了图快把结果文件直接堆在根目录
2. 把缓存当正式数据保存
3. 把输出文件混在源码目录
4. 把实验脚本长期留在正式目录
5. 用多个名字表达同一种目录职责
6. 文档说一套、目录落地另一套且不修正文档

---

## 十二、更新规则

以下情形必须更新本文件：

1. 项目定位变化
2. 一级目录职责变化
3. 对 `eyes` 的边界定义变化
4. 目录语言策略变化
5. 文件治理纪律变化
6. 长期记忆机制变化

建议在文件末尾追加变更记录。

---

## 十三、变更记录

### 2026-06-22

1. 建立项目中心记忆文件
2. 明确项目主工程与 `eyes` 外部仓库区分治
3. 确立“主工程中文治理、外部仓库保留原名”的目录思想
4. 将本文件定义为长期统一项目思想的中心记忆
